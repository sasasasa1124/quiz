- Daily goalをdefaultでは100で、ユーザーごとに設定するようにする
- 読み上げボタンはanswer sheetsなどTTS機能を使いうるところでは全部共通ヘッダーにボタン追加する。(自動再生するが、終了時ように再生ボタンも追加)
- AI fact checkなどが共通ヘッダーにあるのは変、元通り直感的に呼び出し・作用対象と近い場所に置く
- (実装されていれば不要)TTS音声は基本的に同じ問題・選択肢・解説については都度呼び出しする必要がないので、DB+blob(?)で管理
- TTS生成も試験を作成するときのFill inでoptionalで同時に実行できるようにする
